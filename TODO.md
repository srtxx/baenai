# UI改善タスク一覧 (TODO)

『mog（映えない食事記録）』のUI批評に基づく改善タスクおよび受け入れ条件の一覧です。

---

## タスク一覧

### TASK-01: ヘッダー要素の圧縮統合とファーストビュー最適化
- **優先度**: 高
- **対象ファイル**: `src/components/Header.tsx`, `src/App.css`, `src/App.tsx`
- **概要**:
  - ヘッダー内の「ロゴ・タグライン」「週移動ナビ」「日替わり格言カード」「進捗バー（今週のログ）」が縦に積み重なり画面を圧迫している問題を解消する。
  - 格言と進捗バーの統合、パディングの最適化を行い、標準的なスマートフォン画面（高さ750px〜850px）において、スクロールなしで月曜日から日曜日までの7日間が1画面に収まるようにする。
- **受け入れ条件**:
  - モバイル縦画面表示時、7日間すべての行がスクロールなしでファーストビューに表示されること。
  - 日替わり格言と進捗情報（記録数・パーセンテージ）の視認性が損なわれないこと。
  - 週ナビゲーションの操作性が維持されていること。

---

### TASK-02: ナビゲーションの重複解消
- **優先度**: 高
- **対象ファイル**: `src/components/Header.tsx`, `src/components/BottomNav.tsx`, `src/App.tsx`
- **概要**:
  - ヘッダー右上に配置されている「振り返り」「シェア」ボタンと、ボトムナビゲーションの同等ボタンの重複を解消する。
  - ヘッダーは「タイトル表示」「週移動ナビ」に特化し、画面下部のボトムナビゲーションにアクションを集約する。
- **受け入れ条件**:
  - ヘッダー右上のアクションボタンが整理され、余白がすっきりすること。
  - 「振り返り」「シェア」「設定」「記録」の各モーダルがボトムナビゲーションから確実に起動できること。
  - 画面内の重複コントロールが排除されていること。

---

### TASK-03: UI装飾アイコンのSVG統一（絵文字禁止規約の徹底）
- **優先度**: 中
- **対象ファイル**:
  - `src/components/AddMealModal.tsx`
  - `src/components/MealDetailModal.tsx`
  - `src/components/ShareModal.tsx`
  - `src/components/AchievementsModal.tsx`
  - `src/components/SettingsModal.tsx`
  - `src/components/icons/Icons.tsx`
- **概要**:
  - ボタンラベルやモーダル内の文言に混在している装飾用絵文字（カメラ、メモ、ゴミ箱、虫眼鏡、テーマアイコン等）をすべてSVGアイコンコンポーネントに置換する。
  - 絵文字はユーザーが選択・記録する食事コンテンツデータのみに限定し、UI操作系からは完全に排除する。
- **受け入れ条件**:
  - アプリケーション内のすべてのボタン、ラベル、見出し、説明文からUI装飾用の絵文字が排除されていること。
  - 必要なアイコンは `Icons.tsx` 内のSVGコンポーネントを使用して統一的に描画されていること。
  - 実績モーダル、設定モーダル、シェアモーダル、食事詳細モーダルで視覚的な一貫性が保たれていること。

---

### TASK-04: グリッドセルの視覚的トーン統一と状態表現の明確化
- **優先度**: 中
- **対象ファイル**: `src/components/MealCell.tsx`, `src/App.css`
- **概要**:
  - 絵文字記録時のセル背景色のバラつきを抑え、エクリュ/ホワイトを基調とした落ち着いたトーンに統一する。
  - 「過去の未記録スロット（スキップでもなく未入力）」と「未来のスロット」の視覚的差異を明確にする。
- **受け入れ条件**:
  - 1週間分のグリッドを埋めた際、背景色の散らかりがなく統一感のある落ち着いた表示になること。
  - 本日・過去の未記録スロットと、未来のスロットが一目で明確に判別できること。
  - 写真記録セルと絵文字記録セルが並んだ際の視覚的調和が取れていること。

---

### TASK-05: 記録モーダルの操作性向上（履歴サジェストとスクロール負荷削減）
- **優先度**: 低
- **対象ファイル**: `src/components/AddMealModal.tsx`, `src/hooks/useMeals.ts`, `src/App.css`
- **概要**:
  - 食事記録モーダルで目的の項目を選ぶまでのスクロールやタブ切り替えの手数を削減する。
  - 過去に記録した頻度の高い絵文字を「最近使ったもの」として上部に優先表示する。
- **受け入れ条件**:
  - 記録モーダルを開いた際、主要なアクション（写真、休食、最近の食事）がファーストビュー内でワンタップで操作できること。
  - よく使う食事が自動でサジェストされ、探す手間が削減されること。

---

### TASK-06: タッチターゲットサイズとアクセシビリティの向上
- **優先度**: 低
- **対象ファイル**: `src/App.css`, `src/components/Header.tsx`, `src/components/MealCell.tsx`
- **概要**:
  - 週ナビゲーションの矢印ボタンや閉じるボタン、インジケーターなど、モバイルでの推奨タップターゲット（44×44px以上）を満たしていない要素のヒットエリアを拡張する。
  - 文字コントラスト比が不十分な箇所のカラーを見直し、視認性を高める。
- **受け入れ条件**:
  - すべてのインタラクティブ要素のタップ可能領域が44×44px以上確保されていること（CSSの擬似要素またはパディングによる調整）。
  - 休食セルやサブテキストの文字コントラスト比が向上し、屋外や薄暗い環境でも明瞭に読めること。
